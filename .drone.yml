pipeline:
  deployment:
    image: plugins/ssh
    host: nibble.online.ntnu.no
    user: nibble
    port: 22030
    when:
      event: push
      branch: master
    script:
      - cd /home/nibble/Nibble2
      - git checkout master
      - git pull origin master
